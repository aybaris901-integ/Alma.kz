import OrderPage from './components/OrderPage';

// Minimal host for the drop-in OrderPage example. Replace with your own
// router/layout — the parts that talk to Supabase live in src/lib/api.ts.
export default function App() {
  return <OrderPage />;
}
